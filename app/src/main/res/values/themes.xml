<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrizeWinnerGame" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PrizeWinnerGame" parent="Base.Theme.PrizeWinnerGame" />
    <style name="Theme.PrizeWinnerGame.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.PrizeWinnerGame.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.PrizeWinnerGame.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="Theme.PrizeWinnerGame.Fullscreen" parent="Theme.PrizeWinnerGame">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.PrizeWinnerGame.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.PrizeWinnerGame.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>